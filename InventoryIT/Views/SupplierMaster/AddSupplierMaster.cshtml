@{
	ViewData["Title"] = "Product Supplier Details";
}
<html>
<head>
	<link rel="stylesheet" href="~/ItemSetup/style.css" asp-append-version="true" />
	<style>
		body {
			background-color: white;
		}

		.mt-4 {
			margin-top: 1.5rem !important;
			margin-left: 16px !important;
			width: 115%;
		}

		.bg-warning {
			background-color: !important;
		}

		.card-header {
			background: linear-gradient(to right, #093f82, #2b71c7);
			color: white;
			padding: 8px;
			display: flex;
			justify-content: space-between;
			align-items: center;
			border-radius: 5px;
			height: 42px;
		}

		.card {
			box-shadow: 5px 5px 15px rgba(0, 0, 0, 0.3);
		}
	</style>
	<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script> <!-- Load jQuery -->
	<script type="text/javascript">
		// Wait until the DOM is fully loaded before running the script
		$(document).ready(function () {
			// When the State dropdown changes, update the city dropdown.
			$('#StateId').change(function () {
				var stateid = $(this).val();

				if (stateid) {
					$.ajax({
						url: '@Url.Action("GetCityByState", "SupplierMaster")',  // Controller action to fetch city
						type: 'GET',
						data: { stateid: stateid },
						success: function (data) {
							var cityDropdown = $('#CityId');
							cityDropdown.empty();  // Clear existing options
							cityDropdown.append('<option value="">Select city</option>'); // Default option

							// Populate the city dropdown with new options
							$.each(data, function (index, item) {
								cityDropdown.append('<option value="' + item.value + '">' + item.text + '</option>');
							});
						},
						error: function () {
							alert('Error fetching cities. Please try again.');
						}
					});
				} else {
					$('#CityId').empty();
					$('#CityId').append('<option value="">Select City</option>');
				}
			});
		});
	</script>
</head>
<body>
	<div class="container mt-4">
		<div class="card">
			<div class="card-header bg-warning text-white d-flex justify-content-between">
				<h4>SUPPLIER MASTER DETAILS</h4>
				<a href="/SupplierMaster/SupplierMasterDataTable" class="btn btn-dark">RETURN TO VIEW</a>
			</div>

			<div class="card-body">
				<form action="AddSupplierMaster" method="post">
					@if (TempData["SuccessMessage"] != null && !Context.Request.Query.ContainsKey("reset"))
					{
						<div class="alert alert-success">
							@TempData["SuccessMessage"]
						</div>
					}
					<div class="row mb-3 align-items-center" style="margin-top: 13px;">
						<label class="col-sm-2 col-form-label text-end">Supplier Name :</label>
						<div class="col-sm-10">
							<input type="text" class="form-control" placeholder="Enter Supplier Name" id="txtsuppliername" name="SupplierName">
						</div>
					</div>
					<div class="row mb-3 align-items-center">
						<label class="col-sm-2 col-form-label text-end">Address :</label>
						<div class="col-sm-10">
							<input type="text" class="form-control" placeholder="Enter Address" id="txtsupplieraddress" name="Address">
						</div>
					</div>
					<div class="row mb-3 align-items-center">
						<label class="col-sm-2 col-form-label text-end">Contact Person :</label>
						<div class="col-sm-10">
							<input type="text" class="form-control" placeholder="Enter Contact Person" id="txtsuppliercontactperson" name="ContactPerson">
						</div>
					</div>
					<div class="row mb-3 align-items-center">
						<label class="col-sm-2 col-form-label text-end">SupplierGSTCertificate:</label>
						<div class="col-sm-10">
							<input type="file" class="form-control" name="SupplierGstCertificate">
						</div>
					</div>
					<div class="row mb-3 align-items-center">
						<label class="col-sm-2 col-form-label text-end">Pin No :</label>
						<div class="col-sm-2">
							<input type="text" class="form-control" placeholder="Enter phone no" id="txtsupplierphoneno" name="PinNo">
						</div>
						<label class="col-sm-2 col-form-label text-end">GST No :</label>
						<div class="col-sm-2">
							<input type="text" class="form-control" placeholder="Enter GST No" id="txtgstno" name="GstNo">
						</div>
						<label class="col-sm-2 col-form-label text-end">State :</label>
						<div class="col-sm-2">
							<select name="StateId" id="StateId" class="form-control">
								<option value="" disabled selected>Select state</option>
								@foreach (var state in ViewBag.MastStates)
								{
									<option value="@state.Value">@state.Text</option>
								}
							</select>
						</div>
					</div>
					<div class="row mb-3 align-items-center">
						<label class="col-sm-2 col-form-label text-end">*City :</label>
						<div class="col-sm-2 d-flex">
							<select name="CityId" id="CityId" class="form-control">
								<option value="">--Select City--</option>
							</select>
							<button type="button" class="btn btn-warning" data-bs-toggle="modal" data-bs-target="#cityModal">+</button>
						</div>
						<label class="col-sm-2 col-form-label text-end">Mobile No :</label>
						<div class="col-sm-2">
							<input type="text" class="form-control" placeholder="Enter Mobile No" id="txtsuppliermobileno" name="MobileNo">
						</div>
						<label class="col-sm-2 col-form-label text-end">Phone No :</label>
						<div class="col-sm-2">
							<input type="text" class="form-control" placeholder="Enter Phone No" id="txtsupplieralternateno" name="PhoneNo">
						</div>
					</div>
					<div class="row mb-3 align-items-center">
						<label class="col-sm-2 col-form-label text-end">Email ID :</label>
						<div class="col-sm-10">
							<input type="email" class="form-control" id="txtsupplieremail" placeholder="Enter Email ID" name="EmailId">
						</div>
					</div>
					<div class="row mb-3 align-items-center">
						<label class="col-sm-2 col-form-label text-end">Website :</label>
						<div class="col-sm-10">
							<input type="text" class="form-control" placeholder="Enter Website" id="txtsupplierwebsite" name="Website">
						</div>
					</div>
					<div class="text-center mt-3">
						<button type="submit" class="btn btn-success"><i class="bi bi-house-add-fill"></i>Save</button>
						<button type="reset" onclick="window.location.href = window.location.href + '?reset=true';" class="btn btn-primary"><i class="bi bi-arrow-clockwise"></i>Refresh</button>
					</div>
				</form>
			</div>
		</div>
	</div>

	<!-- City Modal -->
	<div class="modal fade" id="cityModal" tabindex="-1" aria-labelledby="cityModalLabel" aria-hidden="true">
		<div class="modal-dialog">
			<div class="modal-content">
				<div class="modal-header">
					<h5 class="modal-title" id="cityModalLabel">City Details</h5>
					<button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
				</div>
				<div class="modal-body">
					<form>
						<div class="mb-3">
							<label class="form-label">* State Name :</label>
							<select name="StateId" id="StateId" class="form-control">
								<option value="" disabled selected>Select state</option>
								@foreach (var state in ViewBag.MastStates)
								{
									<option value="@state.Value">@state.Text</option>
								}
							</select>
						</div>
						<div class="mb-3">
							<label class="form-label">* City Name :</label>
							<input type="text" name="CityId" class="form-control" id="txtcityname" placeholder="Enter City Name">
						</div>
						<div class="text-center">
							<button type="submit" id="saveCityBtn" class="btn btn-success">Save</button>
							<button type="button" class="btn btn-dark" data-bs-dismiss="modal">Close</button>
						</div>
					</form>
				</div>
			</div>
		</div>
	</div>
</body>
</html>
<script>
	document.getElementById('saveCityBtn').addEventListener('click', function() {
		var stateId = document.getElementById('StateId').value;
		var cityName = document.getElementById('txtcityname').value;

		// Check if fields are empty
		if (stateId && cityName) {
			// Create a data object
			var cityData = {
				StateId: stateId,
				CityId: cityName
			};

			// Send an AJAX POST request to the controller to save the city
			$.ajax({
				url: '@Url.Action("SaveCity", "SupplierMaster")',
				type: 'POST',
				data: cityData,

				success: function(response) {
					// If city is saved successfully, close modal and reload the page or redirect
					$('#cityModal').modal('hide');
					window.location.href = '@Url.Action("AddSupplierMaster", "SupplierMaster")'; // Redirect to your existing form
				},
				error: function() {
					alert("An error occurred while saving the city.");
				}
			});
		} else {
			alert("Please fill in all the fields.");
		}
	});
</script>

